using System;

namespace Puthids.Entities.Critters
{
    public class Brain
    {
        public void Autonomize()
        {
            //// Decision-making loop
            //var loopStart = gameTime.TotalGameTime.TotalSeconds;

            //if (gameTime.TotalGameTime.TotalSeconds > loopStart + 5.0)
            //{
            //    // Randomly choose a direction
            //    FacingDirection = Direction.RandomDirection();

            //    if (FacingDirection == Direction.LEFT)
            //    {

            //    }





                // Walk in that direction until either:
                // 1) you hit a wall
                //      -- in that case, change direction, restart clock
                // 2) two seconds pass
                //      -- do a check: 50% continue, 40% pause for 5 seconds, 10% start walking in opposite direction
            //}
        }
    }
}